import { I<%= className %> } from '@libs/types';
import { useCallback, useRef, useState } from 'react';

import { Page } from '../../components';
import { PATH_DASHBOARD } from '../../routes/paths';
import AddEdit<%= className %>Dialog from '../../sections/<%= fileName %>/add-edit-<%= fileName %>-dialog';
import <%= className %>ListTable, { <%= className %>ListTableRef } from '../../sections/<%= fileName %>/<%= fileName %>-list-table';

function <%= className %>List() {
    const tableRef = useRef<<%= className %>ListTableRef>(null);
    const [selected<%= className %>, setSelected<%= className %>] = useState<I<%= className %> | null>(null);
    const [isDialogOpen, setIsDialogOpen] = useState(false);

    const handleAddEdit = useCallback((item?: I<%= className %>) => {
        setSelected<%= className %>(item || null);
        setIsDialogOpen(true);
    }, []);

    const handleCloseDialog = useCallback(() => {
        setSelected<%= className %>(null);
        setIsDialogOpen(false);
    }, []);

    const handleSubmit = useCallback(() => {
        tableRef.current?.refresh();
        handleCloseDialog();
    }, [handleCloseDialog]);

    return (
        <Page
            title="<%= className %>"
            breadcrumbs={[
                {
                    name: 'Dashboard',
                    href: PATH_DASHBOARD.root,
                },
                { name: '<%= className %>' },
            ]}
        >
            <<%= className %>ListTable
                ref={tableRef}
                onEdit={handleAddEdit}
                onAdd={handleAddEdit}
                onRowClick={handleAddEdit}
            />

            {isDialogOpen && (
                <AddEdit<%= className %>Dialog
                    initialValue={selected<%= className %>}
                    onClose={handleCloseDialog}
                    onSubmit={handleSubmit}
                />
            )}
        </Page>
    );
}

export default <%= className %>List;
